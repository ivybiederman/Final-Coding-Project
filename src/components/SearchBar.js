import React from "react";

// Functional component representing a search bar
function SearchBar() {
    return (
    <div>SearchBar</div> // Render the content of the search bar
    )
}

export default SearchBar; // Export the SearchBar component
